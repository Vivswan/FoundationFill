{
  "name": "foundation-fill",
  "version": "1.0.0",
  "description": "A browser extension to fill text fields with predefined system prompt templates",
  "scripts": {
    "prebuild": "node build/generate-icons.js",
    "build": "node build/esbuild.config.js",
    "clean-build": "node build/clean-rebuild.js",
    "dev": "node build/esbuild.config.js --watch",
    "lint": "eslint src/**/*.ts",
    "typecheck": "tsc --noEmit",
    "generate-icons": "node build/generate-icons.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.246",
    "@typescript-eslint/eslint-plugin": "^6.8.0",
    "@typescript-eslint/parser": "^6.8.0",
    "esbuild": "^0.25.3",
    "esbuild-plugin-copy": "^2.1.1",
    "eslint": "^8.51.0",
    "sharp": "^0.34.1",
    "typescript": "^5.2.2"
  }
}